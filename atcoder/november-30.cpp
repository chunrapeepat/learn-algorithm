#include <iostream>

using namespace std;

int main() {
    int M, D, M2, D2; cin >> M >> D >> M2 >> D2;
    if (M == M2) {
        cout << 0 << endl;
    } else {
        cout << 1 << endl;
    }
    return 0;
}