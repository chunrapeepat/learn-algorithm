#include <iostream>

using namespace std;

int main() {
    int P; cin >> P;

    while (P--) {
        int k, s; cin >> k >> s;
        vector<int> dp(s, 0); s[0] = 1; s[1] = 2;

        for (int i = 2; i < s; ++i) {

        }
    }

    return 0;
}